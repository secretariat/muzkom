<div class="row">
  <ul class="nav nav-menu raleway" role="menu" aria-labelledby="dropdownMenu">
    <% @categories.each_with_index do |category, index|%>
        <li class="dropdown-submenu">
          <%= link_to category.name, categories_list_path(:id => category.id), :class=>"category"%>
          <ul class="dropdown-menu">
            <%category.subcategories.each_with_index do |subcategory, subindex|%>
              <li><%=link_to subcategory.name, category_path(subcategory)%></li>
              <% if subindex != category.subcategories.length-1 %>
                <li class="divider"></li>
              <%end%>
            <%end%>
          </ul>
        </li>
        <% if index != @categories.length-1 %>
          <li class="divider"></li>
        <%end%>
    <%end%>
  <li>
  </ul>
</div>
<div class="row obsolete-products" >
  <div class="col-md-8 p-0">
    <strong><%= link_to "Товары снятые с производства", withdrawn_brands_path%></strong>
  </div>
  <div class="col-md-4 p-0 p-t-5">
    <span class="big-icon bag-grey right"></span>
  </div>
</div>



