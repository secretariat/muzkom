<div class="clearfix">
  <div class="col1of5">
    <%=render "shared/brands_filter"%>
    <%=render "shared/catalog"%>
    <%=render "shared/news"%>
  </div>
  <div class="col3of5">
    <div class="container mhs">
      <div class="mhm mvs">
        <h3 class="mbs"><%= @product.name%></h3>
        <div class="breadcrumb">
          <a href=""><%= @product.subcategory.category.name%></a> /
          <%= link_to @product.subcategory.name, category_path(@product.subcategory)%> /
          <span><%= @product.name%></span>
        </div>

        <div class="clearfix mbm">
          <div class="col2of3">
            <div class="mrm mtl">
              <div class="mbm">
                <span class="muted">цена:</span>
                <span class="product-price">
                  <%= human_price @product.converted_price(session[:currency])%>
                </span>
              </div>
              <div class="mbs">
                <strong>Наличие: </strong> 
                <span class="success"><%= @product.status.name%></span>
              </div>
              <div class="mbl">
                <strong>Производитель: </strong> 
                <a href=""><%= @product.brand.name%></a>
              </div>
              <div class="mbm">
                <%= link_to "Купить", add_to_cart_path(@product), :class=>"buy-button"%>
              </div>
              <div class="clearfix mbm">
                <div class="col1of2">

                </div>
              </div>
            </div>
          </div>
          <div class="col1of3 by-center">
            <a href=""><%=image_for_product(@product)%></a>
          </div>
        </div>
        <div class="clearfix">
          <%= @product.description.html_safe%>
        </div>
      </div>
    </div>
  </div>
  <div class="col1of5">
    <%=render "shared/contacts"%>
    <div class="container">
      <h2><span>Похожие товары</span></h2>
      <div class="pam">
        <% @similar.each do |product|%>
        <div class="by-center mbl">
          <div class="">
            <strong>
              <%= link_to product.name, product_path(product)%>
            </strong>
          </div>
          <div class="mbs">
            <%= image_for_product(product)%>
          </div>
          <strong>
            <%= human_price product.converted_price(session[:currency])%>
          </strong>
        </div>
        <%end%>
      </div>
    </div>
  </div>
</div>